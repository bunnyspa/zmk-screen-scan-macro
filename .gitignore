host/windows/hidapi.dll
__pycache__/
*.pyc
.pytest_cache/

# User-local runtime data - profile.json + reference images created
# wherever the app is run from (see PROFILES_ROOT in host/windows/main.py),
# not source. Anchored to the repo root (leading slash) so it can't
# accidentally match anything under host/.
/profiles/
/samples/